DATABASE_URL="postgresql://user:password@localhost:5432/infinitv8"
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-here"
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth — required for "Continue with Google"
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Email (Resend) — required for verification & password reset
RESEND_API_KEY=""
EMAIL_FROM="INFINITV8 <noreply@infinitv8.com>"

# KYC (Didit) — required for identity verification
DIDIT_API_KEY=""
DIDIT_WEBHOOK_SECRET=""
DIDIT_WORKFLOW_ID=""

# Rate Limiting (Upstash Redis) — required
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""

# WalletConnect — required for wallet connections
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=""

# Smart Contract — required for on-chain investment
NEXT_PUBLIC_CONTRACT_ADDRESS=""
NEXT_PUBLIC_TESTNET_CONTRACT_ADDRESS=""
